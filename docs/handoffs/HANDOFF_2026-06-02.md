# SIGNAL — Session Handoff (2026-06-02)

You are picking up work on **SIGNAL**, a ranked-feed intelligence product (AI / Finance / Semiconductors). Read `CLAUDE.md` at the repo root first — it is authoritative for schema, routes, jobs, and phase state.

## Where things are

- **Canonical clone:** `C:\dev\signal-app\` (the only working copy; holds `.git/`). Always launch from here, never an OneDrive-nested path.
- **Branch:** `main`, clean, up to date with origin.
- **HEAD:** `6eb2b48` — Phase 12s.
- **Shell:** the CC tooling shell is **bash**; Omar's own terminal is PowerShell. Filter `*_TOKEN|*_KEY|*_SECRET` from any env inspection.
- `DATABASE_URL` points at the Railway DB (`roundhouse.proxy.rlwy.net`). Reads are fine; **never run prod write SQL** — go through migrations or provide commands for Omar.

## Recently shipped (last 6 PRs)

| PR | Phase | What |
|----|-------|------|
| #109 | 12s | `events.illustration_url` (migration 0044) + API wire + StoryDetail hero / FeedLead fallback / StoryCard native thumbnail + `apply-illustrations` ops script. 3 native posts illustrated via Higgsfield. |
| #108 | 12r | Native post detail hero (synthesis body) + `/archive` page + `GET /api/v1/stories/native`. |
| #107 | 12q | `getRelatedStories` rewritten to query `events` (dual-table anchor). |
| (12p) | 12p | `searchStories` rewritten to query `events` + GIN index (migration 0043). |
| (12o) | 12o | Branded generator labels + comment counts on feed cards. |
| #106 | 12n | Three-section feed card (hook title + attribution + body). |

## ⚠️ OUTSTANDING POST-MERGE OPS — do these before relying on Phase 12s in prod

Phase 12s (#109) is merged but the prod DB has **not** been migrated and illustrations are **not** applied. Run in order:

```bash
# 1. Adds events.illustration_url column
npm run db:migrate --workspace=backend

# 2. Dry-run — confirms backend/scripts/illustration-manifest.json loads (3 entries)
npm run apply-illustrations --workspace=backend

# 3. Writes the 3 CloudFront illustration URLs into the native rows
npm run apply-illustrations --workspace=backend -- --apply
```

After deploy, open the "AI labor collapse" native post detail and confirm the 16:9 illustration hero renders above the synthesis body.

Also still pending from earlier phases: **Phase 12p search GIN index (migration 0043)** must be applied to prod (`npm run db:migrate`) before search on the live feed behaves. If you run the migrate command above it will apply 0043 and 0044 together — check `db:migrate --dry-run` output first.

## What's next

**Phase 12h — Stripe / billing** is the remaining major pre-launch slot. It replaces the `/upgrade` page's "Coming soon" button with a real checkout flow and activates `tier='pro'` post-checkout. The paywall, tier model (`free` / `pro_trial` / `pro`), and lazy trial-downgrade are already wired (Phase 12g, see CLAUDE.md §9) — 12h is the payment integration on top. There are `stripe:*` skills and a `pricing` skill available.

## Recurring ground rules (every session has used these)

- **Read-only audit first**, then implement.
- **Gates before reporting complete:** `tsc` clean both packages, backend `npm run lint` clean, backend `npm test` (jest), frontend `npm test` (vitest). Current baseline: **backend 85 suites / 1267 passed / 1 skipped**, **frontend 21 files / 117 passed**.
- **Tests ship with features** — adding a feature without tests is a review hard-block.
- **One logical change per commit.** Conventional Commits (`feat(scope):`, `fix(scope):`). Co-author trailer on agent commits.
- **Merge flow:** branch `claude/<slug>` off main → stage only the relevant files (the tree has persistent untracked artifacts: `OneDrive/`, `graphify-out/`, `.playwright-mcp/`, `.claude/`, `pr56-diff.txt` — never stage these) → commit → push → `gh pr create` → `gh pr merge <n> --squash --delete-branch` → `git checkout main && git pull`.

## Key architecture a fresh session needs

- **`events` table is the live content table.** `source_type` distinguishes `'native'` (SIGNAL-authored editorial) from `'ingested'`. The legacy `stories` table (20 hand-curated seed rows) is still read by detail/saves but was removed from feed/search/related.
- **Native synthesis body** lives in `generic_commentary` (= `accessible.thesis + " " + accessible.support`), NOT `context` (which holds generator metadata for native posts).
- **Migrations:** hand-written SQL in `backend/src/db/migrations/` (NOT `backend/drizzle/`), applied by the homegrown runner `npm run db:migrate`. Latest is `0044`. Update `schema.ts` in the same commit.
- **Frontend `Story` type** (`frontend/src/types/story.ts`) is the wire contract; `shapeEvent`/`shapeStory` in `storyController.ts` produce it. New wire fields → update the type + every test fixture (tsc will flag missing required fields).
- **Higgsfield MCP** is available for image generation (`generate_image`, model `nano_banana_2`, ~3 credits per 4K image). Used in 12s to illustrate native posts; the same pattern (query native posts with null `illustration_url` → generate → `apply-illustrations -- --apply`) handles future batches until the Phase 13 API integration.

## First move for the new session

Confirm the post-merge ops above with Omar (run the migration + apply illustrations), OR if he wants to start 12h, begin with a read-only audit of the `/upgrade` page, `requireTier` middleware, and the existing tier model before designing the Stripe integration.
